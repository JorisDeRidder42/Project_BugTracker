@model List<BugTracker.Models.User>
@{
    ViewData["Title"] = "Index";
}
<h2>Users</h2>

<ul>
    @foreach (var user in Model)
    {
        <li>@user.FirstName @user.LastName @user.GeboorteDatum</li>
    }
</ul>